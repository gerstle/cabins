<div class="row">
  <div class="status alert-info">
    <% if !current_user.tier.nil? %>
        <% if current_user.tier.available <= DateTime.now %>
            <div><strong>You can book now!</strong></div>
            <div>You are approved and your tier's booking window has been opened, get on it!</div>
        <% else %>
          <div><strong>Your pre-registration has been approved!</strong></div>
          <div>Your pre-registration has been processed and you will be able to book accommodations starting on <strong><%= current_user.tier.available.strftime("%a %-d %b %Y %T %Z") %></strong></div>
        <% end %>
    <% else %>
      <div><strong>You are pre-registered and waiting approval!</strong></div>
      <div>Your pre-registration has not yet been processed. Please contact <a href="mailto:fnfcabins@gmail.com">fnfcabins@gmail.com</a> if you think something bad has happened.</div>
    <% end %>
  </div>
</div>
