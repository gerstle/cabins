<div class="row">
  <h3>Creating a new <%= @post.category %> post</h3>
</div>
<div class="row">
  <div class="col-md-10 col-md-offset-1">
    <%= form_for(@post) do |f| %>
        <%= render 'shared/error_messages' %>
        <%= hidden_field(:post, :category) %>
      <div class="field">
        <%= f.label :title %><br>
        <%= f.text_field :title %>
      </div>
      <div class="field">
        <%= f.label :body %><br>
        <%= f.text_area :body, class: 'body' %>
      </div>
      <div class="actions">
        <%= f.submit "Create", class: 'btn btn-primary' %>
      </div>
    <% end %>
  </div>
</div>